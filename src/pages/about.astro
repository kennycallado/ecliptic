---
import Layout from '$components/client/layouts/Layout.astro';

const needs = { auth: false, roles: [] };
---

<Layout
  needs={needs}
  title="About"
  page="How we are?"
  back={{enabled: false}}>

</Layout>
