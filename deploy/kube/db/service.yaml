apiVersion: v1
kind: Service

metadata:
  name: webslab-db-svc

spec:
  ports:
    # TODO: change
    - name: http
      port: 80
      targetPort: 8000

  selector:
    app: webslab-db

---
apiVersion: v1
kind: Service

metadata:
  name: webslab-db-otel-svc

spec:
  type: ExternalName
  externalName: metrics-svc.metrics.svc.cluster.local

  ports:
    - port: 3000
      name: grafana
    - port: 4317
      name: otel-grpc
    - port: 4318
      name: otel-http
