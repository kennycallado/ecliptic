apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: webslab-app-ing
  annotations:
    spec.ingressClassName: traefic
    cert-manager.io/issuer: webslab-issuer
    traefic.ingress.kubernetes.io/router.tls: "true"

spec:
  tls:
    - hosts:
        - ecliptic.ipsitec.es
      secretName: webslab-app-base-ing

  rules:
    - host: ecliptic.ipsitec.es
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: webslab-app-svc
                port:
                  number: 80
